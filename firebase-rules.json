{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth != null && auth.uid === $userId",
        ".write": "auth != null && auth.uid === $userId",
        "profile": {
          ".validate": "newData.hasChild('username') && newData.hasChild('email')"
        },
        "cards": {
          "$cardId": {
            ".validate": "newData.hasChild('name') || newData.hasChild('company')"
          }
        },
        "leads": {
          "$sessionId": {
            ".write": true,
            ".validate": "!data.exists() || newData.exists() || (auth != null && auth.uid === $userId)",
            "notes": { ".write": "auth != null && auth.uid === $userId", ".validate": "newData.isString() && newData.val().length < 5000" },
            "tags": { ".write": "auth != null && auth.uid === $userId" },
            "status": { ".write": "auth != null && auth.uid === $userId", ".validate": "newData.isString() && newData.val().length < 20" },
            "reminder": { ".write": "auth != null && auth.uid === $userId" },
            "card": { ".write": true, ".validate": "newData.isString() && newData.val().length < 50" },
            "lastFollowup": { ".write": "auth != null && auth.uid === $userId" }
          }
        },
        "taps": {
          ".read": true,
          "$sessionId": {
            ".write": "!data.exists()",
            ".validate": "newData.hasChild('ts')",
            "lead": { ".write": "!data.exists()" },
            "card": { ".write": true },
            "status": { ".write": true },
            "source": { ".write": true },
            "timeout": { ".write": true }
          }
        },
        "analytics": {
          ".read": true,
          "$cardId": {
            "views": {
              "$pushId": {
                ".write": "!data.exists()",
                ".validate": "newData.hasChild('ts') && newData.hasChild('action')"
              }
            },
            "clicks": {
              "$pushId": {
                ".write": "!data.exists()",
                ".validate": "newData.hasChild('ts') && newData.hasChild('action')"
              }
            }
          }
        },
        "settings": {
          "$key": {
            ".write": "auth != null && auth.uid === $userId"
          }
        },
        "latest": {
          ".read": true,
          ".write": "!data.exists() || newData.exists()",
          ".validate": "newData.hasChild('sessionId') && newData.hasChild('ts')"
        },
        "subscription": {
          ".read": "auth != null && auth.uid === $userId",
          ".write": false
        }
      }
    },
    "usernames": {
      ".read": true,
      "$username": {
        ".write": "!data.exists() || (auth != null && data.val() === auth.uid)",
        ".validate": "newData.isString() && newData.val().length > 0"
      }
    },
    "public-tokens": {
      ".read": true,
      "$token": {
        ".write": "auth != null && newData.child('userId').val() === auth.uid",
        ".validate": "newData.hasChild('userId') && newData.hasChild('cardId')"
      }
    },
    "public-nfc": {
      ".read": true,
      "$nfcToken": {
        ".write": "auth != null && newData.val() === auth.uid",
        ".validate": "newData.isString()"
      }
    },
    "waitlist": {
      ".read": "auth != null",
      "$pushId": {
        ".write": "!data.exists()",
        ".validate": "newData.hasChild('email') && newData.hasChild('ts')"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
